\documentclass{homework}

% Set up your name, the course name and the homework set number.
\basicinformation{
	username={Ran Alicia Jing},
	course={Electrodynamics},
	setnumber=1}
\begin{document}% this also prints the header.

\section{Electrostatics}

\problem
Find the electric field of several typical distribution by integration using Coulomb's law: 
\begin{enumerate}
	\item $z$ above one end of a straight line segment of length $L$ that carries uniform line charge $\lambda$.
	\item $z$ above the center of a circular loop of radius $r$ that carries a uniform line charge $\lambda$.
	\item $z$ above a square loop (side $a$) carrying uniform line charge $\lambda$.
	\item $z$ above the center of a flat circular disk of radius $R$ carrying uniform surface charge $\sigma$.
\end{enumerate}

\begin{solution}
	\vspace{10em}
	\tcblower
\end{solution}

\problem
Find the electric field a distance $z$ from the center of a spherical surface
of radius $R$ that carries a uniform charge density $\sigma$. Treat the case $z<R$ (inside) as well as $z>R $(outside).
\textbf{Then} use the result to find the field inside and outside a solid sphere of radius $R$ carrying uniform volume charge density $\rho$.
\begin{solution}
	\vspace{7.5em}
	\tcblower
\end{solution}

\problem
Suppose the 

\end{document}